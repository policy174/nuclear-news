# 절대 GitHub에 올라가면 안 되는 것들
.env
*.local

# 로컬 검색 결과 (용량 크고 공개 불필요)
raw/
*-raw-*.md

# 발송 결과 임시 파일 (같은 job 안에서 --send → --confirm 전달용, 커밋 불필요)
outbox_result.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
