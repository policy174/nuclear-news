# 원자력 뉴스봇 — AI 전달용 전체 레퍼런스

> **이 문서의 용도**: 이 봇을 전혀 모르는 사람(또는 AI)에게 봇의 모든 것을 설명하기 위한
> 단일 소스 문서. 초보자용 설명 자료·발표·글을 만들 때 이 문서 하나만 입력으로 주면 되도록
> 자기완결적으로 작성됨. 2026-07-30 기준 실제 코드·설정·운영 기록에서 추출한 사실만 담음.
>
> **⚠️ 이 문서를 받은 AI가 지킬 것**:
> - 토큰·비밀번호·채팅 ID·저장소 주소는 이 문서에 없고, 어떤 산출물에도 넣지 말 것.
> - 운영 수치는 2026-07-29 저장소 기준 스냅샷 — "당시 기준"임을 명시할 것.
> - 없는 기능을 지어내지 말 것. 특히 "피드백 버튼"은 과거에 있다가 **삭제된** 기능.

---

## 1. 한 줄 정의

**전 세계 원자력 뉴스를 매시간 자동 수집하고, AI(Gemini)로 선별·분석해서,
매일 아침 텔레그램으로 "투자 관점 카드 브리핑"을 보내주는 무인 봇.**

서버 없음, 운영비 0원, 코드는 GitHub에 올려두면 GitHub Actions가 정해진 시각에 대신 실행.

## 2. 누가, 왜 만들었나

- **만든 사람**: 한국수력원자력(한수원) 원자력정책 부서에서 일하는 **비개발자** 실무자.
  코딩을 직접 배운 게 아니라 **AI 코딩 도구(Claude Code)에게 지시해서** 만들었음.
- **문제의식**: 원자력 정책 업무를 하려면 해외 전문지·국제기구·국내 보도자료를 매일
  훑어야 하는데, 사람이 하면 매일 30분~1시간이 사라지고 빠뜨리는 이슈가 생김.
- **목표**: 영어권 1차 소스에서 새 이슈를 남들보다 빨리 캐치. 부수 목적으로 개인 투자
  판단(원자력·에너지 테마)에 쓸 시그널 확보.
- **의미**: "비개발자도 AI를 레버로 쓰면 자기 업무 자동화 시스템을 만들 수 있다"의 실증 사례.
  약 2개월(2026년 5월~7월)간 실제 운영하며 시행착오를 거쳐 현재 형태로 진화함.

## 3. 사용자가 실제로 받는 것 (겉모습)

텔레그램 채팅방에 알림이 딱 **주 6번** 온다:

1. **매일 아침 07:25 (KST) — 일일 카드 브리핑 1건**
   - 국내 뉴스 최대 3장 + 해외 뉴스 최대 6장의 "분석 카드" 묶음.
   - 카드 1장의 구조:
     - **무슨 일** — 사실 요약 (한국어)
     - **왜 중요** — 정책·산업 맥락
     - **💰 투자 관점** — 테마·수혜 메커니즘·시계(時界)·확신도. 구조화된 필드를
       파이썬이 문장으로 조립. 근거가 약하면 그 줄 자체를 생략. **개별 종목 추천은 금지.**
     - **🇰🇷 한국 시사점** — 한국 관련성이 있을 때만 붙는 보조 렌즈
     - **✅ 출처 공신력 배지** — 공식기관·전문지면 표시
2. **금요일 17:00 — 주간 판세 리포트 1건**
   - 개별 기사 재나열이 아니라 한 주의 **판세**: 정책 변화 / 테마 강약 / watchlist / 소스 공백.

국내 3건 캡이 해외(6건)보다 적은 이유: 국내 뉴스는 사용자가 업무상 다른 경로로도 접하기
때문에 의도적으로 축소.

## 4. 전체 구조 — 3개의 자동 실행 작업

GitHub Actions(깃허브가 제공하는 무료 예약 실행 서비스)에 워크플로 3개가 등록되어 있고,
각각 정해진 시각에 파이썬 스크립트를 실행한다:

| 워크플로 | 주기 | 실행 파일 | 역할 |
|---|---|---|---|
| `crawl` | **매시간 정각** | `news_bot.py` | 수집 → 중복 제거 → Gemini 일괄 분류 → 발송 대기 큐에 적재 |
| `daily-brief` | **매일 07:25 KST** | `daily_brief.py` | 큐에서 랭킹으로 선별 → 카드 합성 → 텔레그램 발송 |
| `weekly` | **금 17:00 KST** | `weekly_bot.py` | 한 주치 데이터로 판세 리포트 (Gemini 주 1회 호출) |

핵심 설계: **수집(조용히 쌓기)과 발송(하루 1번)을 분리**. 매시간 도는 crawl은 절대
알림을 보내지 않고 큐에만 쌓는다. 알림 피로를 없애는 구조.

## 5. 수집 소스 (입력)

| 채널 | 구체적 소스 | 방식 |
|---|---|---|
| 해외 전문지 RSS | IAEA Top News, World Nuclear News(WNN), ANS Nuclear Newswire | RSS 직접 구독 |
| 한국 기관 보도자료 | 한수원, 원자력안전위원회, 산업통상자원부, 원자력연구원 | Google News `site:` 검색 RSS (`when:2d` 신선도 제한) |
| 국내 언론 원자력 보도 | "한수원 OR 원안위 OR 원전수출 OR i-SMR OR 신한울 …" 타깃 키워드 | Google News 검색 RSS (`when:1d`) |
| 네이버 뉴스 검색 | `keywords.json`의 토픽 2개(정책 / SMR), 각 30~50개 키워드 | Naver 검색 API |
| 이메일 뉴스레터 | ANS 'Nuclear News Daily' (매일 밤 도착하는 구독 뉴스레터) | Gmail IMAP으로 본문에서 외부 매체 링크만 추출, 원본 메일은 저장 안 함 |
| (휴면) 소셜 | Reddit/X/YouTube — `last30days` 엔진 기반 | 코드는 남아있으나 자동 실행에서 제외 (수동 실행 전용, §12 시행착오 참조) |

노이즈 사전 차단: 제목 패턴(`[인사]`, `[부고]`, 채용 공고, 체육대회, "원자력병원" 등)과
negative 키워드로 무관 기사를 수집 단계에서 걸러냄.

## 6. 처리 파이프라인 (crawl 한 사이클)

```
수집 (RSS + Naver + 이메일)
  → URL 정규화 dedup       # utm 등 트래커 파라미터 제거 후 해시 비교, sent.json에 14일 보존
  → Gemini batch 큐레이션   # 기사 10건을 한 번의 API 호출로 일괄 처리
      · importance 분류: must_read / nice_to_know / noise
      · 한국어 요약 생성
      · 랭킹용 feature 5종 채점 (0~3점): 한국 관련성, 시장 중요도,
        정책 중요도, 새로움(novelty), 근거 강도
      · 사건 유형 태깅: 정책결정/규제/계약/사고·안전/마일스톤/기업/시장/리서치/의견
  → digest_queue.json 적재  # 발송 대기 큐
```

- batch 방식인 이유: 초기엔 기사당 2회 호출 + 10초 대기 → 무료 API 한도 초과(429 에러)
  빈발. 10건 묶음 1회 호출로 바꿔 호출량을 **약 1/20**로 줄임.
- Gemini 키가 없거나 호출 실패 시: 도메인 기반 fallback으로 분류(봇이 죽지 않음).

## 7. 랭킹 — "설명 가능한 점수식" (이 봇의 자랑거리)

**LLM에게 순위를 통째로 맡기지 않는다.** LLM은 feature 점수(0~3)만 매기고,
최종 점수는 파이썬이 `ranking_config.json`의 가중치로 계산한다:

```
점수 = 중요도 기본점 (must_read +10 / nice +5)
     + 사건유형 가점 (정책결정·계약·사고 +6 > 마일스톤 +4 > 의견 0)
     + Σ (feature 점수 × 가중치)   # 예: 한국 관련성 ×1.2
     + 출처 공신력 보너스 (tier1 +3.0 / tier2 +1.5)
     − 시간 감쇠 (큐 적재 후 12시간마다 −0.5, 최대 −3)
     − 다양성 페널티 (같은 테마 2건 초과 시 −2.5)
```

- **왜 이 기사가 뽑혔지?** → `delivery_log.jsonl`의 `breakdown` 필드에 항목별 점수
  내역이 전부 기록됨. 블랙박스가 아님.
- **가중치 조정** → JSON 숫자만 고치면 됨. 코드 수정 불필요 (비개발자 운영 원칙).
- 출처 공신력은 `sources.json` 화이트리스트: tier1 = WNN·NucNet·IAEA·OECD NEA·미
  NRC/DOE 등 전문·공식기관, tier2 = Reuters·Bloomberg·FT·연합 등 신뢰 일반 매체.
- 선별 안 된 기사는 폐기가 아니라 큐에 남아 **다음날 재경쟁** (시간 감쇠로 서서히 밀리고
  3일 후 자동 정리).

## 8. 발송 원자성 — outbox 패턴 (중복 발송 잡은 핵심 설계)

과거 최대 골칫거리: "발송은 됐는데 상태 저장(push)이 실패 → 봇은 안 보낸 줄 알고
다음날 같은 브리핑 재발송". 이를 4단계 프로토콜로 해결:

```
① plan    — 기사 선별, outbox.json에 발송 계획(pending) 기록, 큐에서 해당 기사만 hash 단위 제거
② claim   — outbox+큐를 발송 **전에** git push. 5회 재시도, 전부 실패하면 발송 자체를 포기
             (큐는 원격에 그대로 → 내일 재시도. 중복보다 하루 늦는 게 낫다는 판단)
③ send    — outbox의 pending 항목만 발송. 재실행돼도 sent는 건너뜀
④ confirm — 발송 결과·delivery_log를 push (충돌 시 재병합 재시도)
```

추가 안전장치: 36시간 지난 pending은 재발송하지 않음(stale 컷). 같은 날 워크플로를
다시 돌려도 중복 발송 없음(멱등).

## 9. 데이터 저장 — "git이 데이터베이스"

별도 DB 서버 없이 JSON 파일을 git 저장소에 커밋하는 방식:

| 파일 | 내용 |
|---|---|
| `sent.json` | 수집 dedup용 URL 해시 (14일 보존) |
| `curated.json` | 큐레이션 결과 캐시 (14일) — weekly 리포트의 입력 |
| `digest_queue.json` | 발송 대기 큐 (3일 자동 정리) |
| `outbox.json` | 오늘의 발송 계획·상태 (pending/sent/failed) |
| `delivery_log.jsonl` | 발송 이력 + 점수 breakdown |

장점: 무료, 이력이 커밋 히스토리로 전부 남음, 롤백 쉬움. 매시간 crawl과 아침 brief가
동시에 push하면 충돌 가능 → 재시도 로직으로 방어 (§8).

## 10. 기술 스택과 비용

| 항목 | 선택 | 비용 |
|---|---|---|
| 언어 | Python 3.12, **표준 라이브러리만** (urllib 직접 호출, 외부 패키지 의존 최소화) | 0 |
| 실행 인프라 | GitHub Actions (public repo 무료 티어) | 0 |
| LLM | Google Gemini (`gemini-2.5-flash`, REST 직접 호출 + 429 백오프 내장 wrapper) | 무료 티어 (한도 빠듯 → 유료 전환 검토 여지) |
| 발송 | Telegram Bot API (parse_mode=HTML) | 0 |
| 국내 검색 | Naver 검색 API | 무료 티어 |
| 이메일 수집 | Gmail IMAP (표준 라이브러리 imaplib) | 0 |
| DB | 없음 — git 저장소가 DB (§9) | 0 |

**월 운영비 0원.** 테스트 수트 약 57~68개 (`python -m unittest discover tests`),
외부 호출 없이 돌아가도록 작성됨. `python daily_brief.py --dry-run`으로 발송 없이
브리핑 미리보기 가능. `python metrics.py`로 precision@k·noise rate 등 품질 지표 산출
(표본 부족 시 insufficient_data 표시로 성급한 결론 방지).

## 11. 비개발자 운영 원칙 — "설정은 JSON, 코드는 불가침"

운영자가 코드를 못 고쳐도 봇을 조정할 수 있도록, 바뀔 만한 것은 전부 JSON으로 분리:

| 조정하고 싶은 것 | 편집할 파일 | 코드 수정 |
|---|---|---|
| 검색 키워드 추가/삭제 | `keywords.json` | 불필요 |
| 랭킹 가중치 (뭘 더 중요하게 볼지) | `ranking_config.json` (모든 항목에 한국어 주석) | 불필요 |
| 출처 공신력 매체 추가 | `sources.json` | 불필요 |
| 과거 보고서 지식베이스 | `reports_kb.json` (보고서 추천 정밀화) | 불필요 |

편집 → GitHub 웹에서 직접 커밋 → 다음 실행부터 적용. 실제 운영자는 git CLI 대신
GitHub 웹 편집을 쓴다.

비밀값(GitHub Secrets): 필수 = 텔레그램 토큰·채팅 ID, 네이버 API 키.
선택 = Gemini 키(없으면 fallback 발송), IMAP 계정(없으면 뉴스레터 수집 스킵).
**키가 없어도 해당 기능만 조용히 꺼지고 봇은 계속 돈다** (graceful degradation).

## 12. 진화의 역사 — 시행착오 타임라인 (설명 자료의 스토리 뼈대)

이 봇의 진짜 교훈은 "한 번에 잘 만든" 게 아니라 **운영하며 깨지고 고친 기록**:

| 시기 | 사건 | 교훈/해결 |
|---|---|---|
| 2026-05 | v1: 토픽별 알림이 하루 3~4개 별도 발송 — **알림 폭탄** | 알림은 적을수록 좋다 → 하루 1개 카드 브리핑으로 통합 |
| 2026-06-14 | 헤드라인 나열 → **분석 카드**로 전환 (투자 관점 기본 렌즈 도입) | "무슨 일이 있었나"보다 "그래서 뭐가 중요한가"가 가치 |
| 2026-06-14 | 로컬 개발본과 운영 저장소가 완전 별개 히스토리로 갈라져 있던 것 발견 (한 달간 배포 안 됨) | 배포 확인 없는 개발은 없는 개발. 통합 재배포로 정리 |
| 2026-07-09 | 소셜(Reddit/X/YouTube) 검색이 8토픽×7분 = **60분 타임아웃** → 브리핑 발송 자체가 중단 | 무거운 기능이 핵심 기능을 죽이면 잘라낸다 → 소셜을 자동 경로에서 제외 |
| 2026-07-09 | push 충돌 시 `push \|\| echo`로 조용히 포기 → 큐가 안 비워져 **다음날 중복 발송 반복** | 침묵하는 실패가 최악 → 재시도 5회 + outbox 원자성(§8)으로 재설계 |
| 2026-07-10 | **한국 기사 0건 미스터리**: Google News 검색 RSS가 '관련도순'이라 최신 필터에서 전멸 (실측 100건 중 95건이 1주 이상 지난 기사) | 데이터로 원인 실측 → 쿼리에 `when:1d` 연산자 추가로 해결 |
| 2026-07-10 | Gemini 무료 한도 초과(429) 빈발 | 기사당 2회 호출 → 10건 배치 1회 (호출 ~1/20) |
| 2026-07-11 | 이메일 뉴스레터 수집: 트래커 링크가 HEAD 요청을 405로 거부 → 링크 언랩 전멸 | HEAD→GET 재시도. "표준대로 안 움직이는 서버"는 흔하다 |
| 2026-07-13 | 대개편: 설명가능 랭킹, outbox 패턴, 투자 관점 구조화, weekly 회귀 버그(스키마 변경 후 매주 0건으로 죽어있던 것) 수정, 테스트 60여 개 | LLM은 feature 추출기로 격하, 판단 로직은 코드로 |
| 2026-07-13 | 카드별 피드백 버튼(중요/노이즈/투자) 도입 | 브리핑만 어지럽히고 실제 사용 0건 → **07-16 완전 삭제**. 기능은 빼는 것도 개선 |

## 13. 운영 수치 스냅샷 (2026-07-29 저장소 기준)

- 누적 수집·분류: **459건**
- 그중 noise 판정(발송 안 함): **약 55%**
- 실제 발송된 브리핑 카드 이력: **9건 로그** (통합 브리핑 체제 이후)
- 알림: 하루 1회 + 주 1회 (초기 v1은 하루 3~4회)
- 이 수치는 갱신됨 — 최신값은 `curated.json`·`delivery_log.jsonl` 집계로 재산출 가능.

## 14. 초보자 설명용 용어 미니 사전

| 용어 | 쉬운 설명 |
|---|---|
| RSS | 사이트가 "새 글 목록"을 기계가 읽기 좋게 제공하는 표준 형식. 구독하면 새 글을 자동으로 받아옴 |
| GitHub Actions | 깃허브가 제공하는 "정해진 시각에 내 코드를 대신 실행해주는" 무료 로봇 |
| cron | "매일 몇 시에 실행"을 지정하는 예약 문법 |
| API | 프로그램끼리 대화하는 창구. 텔레그램 API = "봇이 메시지를 보내는 공식 통로" |
| LLM / Gemini | 대규모 언어모델. 이 봇에선 구글의 Gemini가 기사 분류·요약·채점 담당 |
| dedup | 중복 제거. 같은 뉴스가 여러 매체에 실려도 1건으로 합치는 것 |
| 큐(queue) | 발송 대기 줄. 수집된 기사가 아침 브리핑 때까지 기다리는 곳 |
| outbox 패턴 | "보내기 전에 보낼 계획부터 기록"해서 중복 발송을 막는 설계 |
| 429 에러 | "너무 자주 호출했으니 천천히 와라"는 API의 거절 신호 |
| fallback | 주 기능이 실패했을 때 대신 작동하는 차선책 |
| 멱등(idempotent) | 같은 작업을 두 번 실행해도 결과가 한 번과 같음 — 재실행해도 중복 발송 없음 |

## 15. 이 봇이 보여주는 핵심 메시지 (설명 자료의 결론 후보)

1. **비개발자 + AI = 실전 자동화 시스템.** 코드를 배우는 대신 AI에게 지시하고,
   운영하며 발견한 문제를 다시 AI에게 설명해 고치는 반복.
2. **LLM에게 다 맡기지 않는다.** 분류·요약·채점만 LLM, 순위·발송·상태관리는
   결정적 코드. 그래서 "왜 이 기사가 뽑혔는지" 항상 설명 가능.
3. **운영이 설계를 만든다.** outbox 패턴, batch 호출, when: 연산자 — 전부 책이 아니라
   장애에서 나옴.
4. **비용 0원.** GitHub Actions + 무료 API 티어 + git-as-DB 조합이면 서버 없이
   개인용 자동화가 완결됨.
5. **빼는 것도 개선.** 알림 4개→1개, 소셜 경로 차단, 피드백 버튼 삭제.
